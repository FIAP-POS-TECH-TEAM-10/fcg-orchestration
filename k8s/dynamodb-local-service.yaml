apiVersion: v1
kind: Service
metadata:
  name: dynamodb-local
  namespace: fcgames
  labels:
    app: dynamodb-local
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  selector:
    app: dynamodb-local
